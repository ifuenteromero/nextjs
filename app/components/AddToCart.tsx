'use client';

const AddToCart = () => {
	return (
		<button
			className='btn btn-primary'
			onClick={() => console.log('add to cart')}
		>
			Add to cart
		</button>
	);
};

export default AddToCart;
